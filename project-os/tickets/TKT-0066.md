---
id: TKT-0066
title: スケジュール完了反映バグ修正と完了カード視覚改善
status: implementation_ready
goal: 調理完了後にスケジュールのステータスが正しく「完了」に更新されない問題を修正し、完了したスケジュールカードの視覚的表示を改善する
acceptance:
  - スケジュールから「調理を開始」したレシピを「調理を完了」すると、スケジュールのステータスが「完了」に更新される
  - 翌日に前日登録したレシピを調理しても、正しく該当スケジュールが「完了」になる
  - 完了したスケジュールカードは緑系の背景色（bg-emerald-50）で表示される
  - verify がパスする
required_evals:
  - bug_fix
  - ui_component_update
eval_selection_mode: auto
changed_paths:
  - app.html
required_gates:
  - spec_ready
  - implementation_ready
  - verify_passed
  - manual_smokes_done
  - review_ready
  - report_ready
related_specs:
  - SPEC-0020-consumption-adjustment
related_artifacts:
  - artifacts/TKT-0066/verify.json
  - artifacts/TKT-0066/manual-smokes.md
  - artifacts/TKT-0066/review.md
  - artifacts/TKT-0066/report.md
owner_role: implementer
owner_notes:
  - verify は `python3 -c "import html.parser; html.parser.HTMLParser().feed(open('app.html').read())" && grep -q 'executeGAS' app.html && grep -q 'GAS_URL' app.html && echo 'VERIFY_PASSED'`
  - スケジュール完了は `state.pendingSync.scheduleUpdates` に積む
  - alert/confirm/prompt は使用しない
---

# Summary

調理完了後のスケジュールステータス更新が日付不一致で失敗するバグを修正し、完了済みスケジュールカードの視覚的表示を改善する。

## 実装メモ

- プロジェクト名: Stock Master（料理レシピ・食材管理アプリ）
- 正本: `app.html`, `要件定義書.md`, `AGENTS.md`
- 生成物: GASデプロイ後のWebアプリ、Spreadsheet内データ

## 残リスク

- `openCookingViewer` を直接呼んだ場合（スケジュール経由以外）は `_cookingScheduleItemId` が設定されないため、従来通り「今日の日付 + recipeId」でフォールバック検索する
