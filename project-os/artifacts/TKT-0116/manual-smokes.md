# TKT-0116 Manual Smokes

AI確認:

- `storage_locations` table、index、trigger、RLS policyをmigrationに追加した。
- 保存場所を在庫画面から追加できる。
- 使用中の保存場所は削除ボタンが無効になる。
- 在庫/登録待ちフォームの保存場所入力に候補リストを付けた。
- 既存の在庫、登録待ち、写真AI解析テストは通過している。

ユーザー確認が必要:

- Supabaseへmigrationを適用した環境で保存場所追加/削除を確認する。
- 実データの保存場所が候補に出ることを確認する。
- 使用中の保存場所が削除できないことを確認する。
