# TKT-0176 手動確認

実施日時: 2026-06-05 19:45 JST

## 確認結果

- `http://localhost:3000` をBrowserで開き、ログイン済み画面で食材管理を表示できることを確認した。
- 食材追加から手動追加モーダルを開き、食材画像UI（プレビュー、画像を選ぶ、同じ食材名にも使う、選択を取り消す）が表示されることを確認した。
- 通常幅で画像UIの幅は約639px、ボタンははみ出しなし。
- スマホ幅相当（390x844）で画像UIの幅は約310px、各ボタンはviewport内に収まり、文字の重なりなし。
- 画像は非公開 `photos` バケットへ保存し、DBにはStorage pathだけを保存する実装であることをコード確認した。
- `user_ingredient_images` は本人限定RLSと `user_id + normalized_name` 一意制約を持つことをmigrationで確認した。

## 未実施

- 実Storageへの写真アップロードは本番/共有DBへ副作用が出るため実行していない。アップロード処理は自動テストでmock検証済み。
- Supabase migrationのリモート反映は未実施。反映する場合は別途Supabase Pushが必要。
