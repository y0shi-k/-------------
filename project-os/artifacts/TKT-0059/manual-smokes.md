# TKT-0059 Manual Smokes

- [ ] 在庫編集で `豆腐 3パック` に `1 パック = 150 g` を設定し、同期前に未同期バーが出る。
- [ ] レシピ `豆腐 150g` の料理完了で、消費量調整に `レシピ: 150g → 在庫消費: 1パック` が表示される。
- [ ] 確定後、豆腐の在庫が `2パック` になる。
- [ ] 在庫 `豆腐 450g` に同じ換算を設定し、レシピ `豆腐 1パック` の料理完了で `300g` になる。
- [ ] 換算未設定で `豆腐 3パック` / レシピ `150g` の場合、今回消費欄に `1パック` を入力して `2パック` にできる。
- [ ] 同一単位 `500g` / `150g` は従来通り `350g` になる。
- [ ] 同期ボタン実行後、Spreadsheetの `単位換算JSON` 列に換算JSONが保存される。
- [ ] 代替設定中の行は、上部に元の材料名と数量、矢印、下部に代替品名と在庫消費数量が表示される。

## Static Checks

- [x] 標準 verify passed
- [x] JS syntax check passed
- [x] 双方向換算式 check passed
- [x] native dialog / direct write scan reviewed
- [x] 代替表示変更後の標準 verify / JS syntax check passed
