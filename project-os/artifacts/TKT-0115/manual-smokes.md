# TKT-0115 Manual Smokes

AI確認:

- 在庫を保存場所、種別、期限有無、並び順で絞り込める。
- 在庫カードから `使い切り` を実行できる。
- 登録待ちと在庫でチェック選択できる。
- 登録待ちは選択分を一括削除できる。
- 写真AI解析、登録待ち追加、在庫確定の既存テストは通過している。

ユーザー確認が必要:

- スマホ実機またはモバイル表示で、絞り込み欄と一括操作ボタンが押しやすいこと。
- 実データで保存場所の候補が期待どおりに並ぶこと。
- `使い切り` を数量0として残す運用で問題ないこと。
