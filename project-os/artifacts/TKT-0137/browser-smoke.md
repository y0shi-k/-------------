# Browser Smoke

Status: partial

- Started the Next.js dev server at `http://localhost:3001`.
- Port `3000` was already in use, so Next.js selected `3001`.
- The in-app Browser tool was not available through tool discovery in this session, so an automated screenshot smoke test could not be completed.
- Static verification, typecheck, tests, and production build passed.

Manual follow-up:

- Open `http://localhost:3001`.
- Log in if redirected.
- Switch the bottom navigation to `料理・記録`.
- Confirm the Canvas-like summary cards, tab row, timeline, calendar, insights, and bottom navigation spacing.
