# TKT-0119 Manual Smokes

AI確認:

- レシピ一覧に検索入力を追加した。
- レシピ一覧に並び順を追加した。
- レシピ削除を2段階操作にした。
- レシピ削除は本人の `recipes` 行だけを対象にする。
- `npm run test` / `typecheck` / `lint` / `build` は通過している。

ユーザー確認が必要:

- 実Supabase環境でレシピ検索、並び替え、削除を確認する。
- 削除後に献立選択で不整合が出ないことを確認する。
