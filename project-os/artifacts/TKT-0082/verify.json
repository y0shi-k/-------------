{
  "ticket": "TKT-0082",
  "status": "passed",
  "checks": [
    {
      "name": "standard_verify",
      "command": "python3 -c \"import html.parser; html.parser.HTMLParser().feed(open('app.html').read())\" && grep -q 'executeGAS' app.html && grep -q 'GAS_URL' app.html && echo 'VERIFY_PASSED'",
      "result": "VERIFY_PASSED"
    },
    {
      "name": "native_dialog_static_check",
      "command": "rg -n \"alert\\(|confirm\\(|prompt\\(\" app.html",
      "result": "no matches"
    },
    {
      "name": "manual_bulk_sync_policy_static_check",
      "command": "rg -n \"showToast|executeGAS\\(payload\" app.html",
      "result": "showToast exists; executeGAS(payload...) remains limited to existing sync/load paths"
    }
  ],
  "notes": [
    "completeRecipe uses scheduleItem.date as the cooking date key when a scheduled item is identified.",
    "Direct cooking without a schedule still falls back to the operation date."
  ]
}
