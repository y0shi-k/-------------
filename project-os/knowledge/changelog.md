# Changelog（完了チケットの1行ログ）

> 完了した TKT を**1行ずつ**記録する追記専用ログ。新しいものを上に足す。
> 詳細の正本は各 `project-os/artifacts/<TKT>/report.md`（と `tickets/completed/`）。ここには概要だけ置く。
> `/finalize` が完了時にここへ1行追記し、`backlog.md` の「現在のフォーカス」からは当該行を消す。
> 「現在のフォーカス」は進行中のみを置く場所。完了サマリをそこに残さない（肥大化防止）。

## 2026-06

- TKT-0240 — ユーザー同義語辞書の複数行グループを Union-Find で推移的統合（「A＝B」「B＝C」で A↔C が切れる潜在バグ修正・実害発生前に対処）。verify pass。
- TKT-0239 — 料理完了の消費ダイアログを開く時点で在庫を再取得（献立ボードの在庫スナップショットがページ初回ロードのまま古く、食材管理での追加・補充が自動マッチングに反映されない不具合の根本対応。豚こま肉↔豚コマ報告の本命）。schema/policy 無変更（eval過剰マッチは report 記録）。残: 実機スモーク（再読込なしで追加在庫が自動選択されるか・解消しなければ単位/分類切り分け）。
- TKT-0238 — 在庫編集の保存場所/単位ピッカーを label ネストから div+span へ（タブレットのタッチで候補ボタンが反応せず「その他」へ化ける不具合。レシピのジャンル欄と同構造に統一）。残: タブレット実機スモーク。
- TKT-0236 追補 — 実機で「選択削除」ボタンが見えない問題を修正（ユーザーフィードバック対応）。原因は既存 CSS `.bulk-toolbar > button { display:none }`（全選択/解除ボタンの意図的非表示）への巻き込まれ。`bulk-delete-button` クラス＋表示例外（`display:inline-flex; margin-left:auto`＝ツールバー右端）で解消。jsdom は CSS 非適用のためテスト green でも検出不能だった（learnings 記録）。再 verify pass。
- TKT-0233 — 認証本番化の適用 runbook（SPEC-0228 ⑥/⑥・イニシアチブ完結）。`docs/runbook/認証本番化の適用手順.md` 新設: 適用順チェックリスト（①migration 3点=ai_usage_events/shopping_items_meal_schedule_link/auth_profiles の `db push`・**未適用デプロイ=全員/pending化の警告**→②Dashboard Auth設定（signup/Confirm email ON・Redirect URLs）→③Vercel→④初代admin SQL（§10b参照）→⑤E2EスモークA〜J=0228〜0232のskipped_checks統合）＋ロールバック（signup即OFF）＋SMTP移行概要。既存ガイド§12に参照1行のみ追記。verify pass。レビューでサブエージェントの verify.json 自作を検出・正規実行で是正（learnings 記録）。残: **本番適用の実作業＝ユーザーがこの手順書どおり実施**。
- TKT-0232 — パスワードリセット（SPEC-0228 ⑤/⑥）。`/forgot-password`（`resetPasswordForEmail`・redirectTo=`/auth/confirm?next=/reset-password`＝0229サニタイズ経由・**未登録メールも成功と同一文言＝存在有無非露出**・レート制限のみ別文言）＋`/reset-password`（recovery セッション前提・確認入力・`updateUser`・日本語エラー変換・未ログインは/login）。routing は pending/disabled の例外に `/reset-password` のみ追加＝リセット後も未承認の /pending 固定不変をテスト固定。login に「忘れた方」リンク。verify pass・レビュー指摘なし。残: 実メールE2E（hosted適用時）・/login error クエリ文言（軽微UX・任意）。
- TKT-0231 — アプリ内管理者画面（SPEC-0228 ④/⑥）。`/admin`（サーバー側で getUser→`fetchAccountRole`→非adminは redirect・admin のみ profiles 全行取得）＋`AdminUserBoard`（pending申請/全ユーザー一覧・承認/拒否/無効化/再有効化・拒否系は確認パネル・自己行は二重抑止）。書き込みは RLS admin ポリシー直 update（service role 不使用）で、payload は `buildStatusUpdate` の status/approved_at/approved_by のみ＝**role/email 送出なしをテスト固定**（0228 の列制限リスクの UI 担保）。導線は PCサイドバー設定ギア下＋設定パネル内（admin限定・主ナビ非昇格）。テスト505件 pass・verify pass・レビュー指摘なし。残: 実機スモーク（承認→利用開始・非admin遮断・RLS実挙動）。
- TKT-0230 — 承認ゲート（SPEC-0228 ③/⑥・承認制の実効性本体）。routing を4状態化（`AuthState`=unauthenticated/pending/disabled/approved・23ケースで全分岐固定）、`account-status.ts` 新設（profiles.status 1クエリ・行なし/エラー/未知値→pending の fail-safe）、middleware 4状態判定、`/pending` ページ（承認待ち/利用停止の文言出し分け＋ログアウト・page側二重ガード）、AI route×2 に非approved 403（usage消費前・多層防御）。`/auth/*` は全状態素通し（確認/recoveryリンク保護）。テスト494件 pass・verify pass・レビュー指摘なし。**注意: migration（TKT-0228）未適用環境へ単独デプロイ禁止＝全員/pending化**。残: 実機3アカウント遷移スモーク（hosted適用時）。
- TKT-0229 — 新規登録申請フロー（SPEC-0228 ②/⑥）。`/signup`（メール＋PW確認入力・日本語エラー変換・成功時「確認メール送信＋承認待ち」案内）＋`/auth/confirm` route handler（`verifyOtp` type許可リスト signup/email/recovery・`next` サニタイズ＝オープンリダイレクト防止・失敗時 `/login?error`）＋`safe-next-path.ts` 新設、routing に `isPublicPath`（/login・/signup・/auth/*）、login に新規登録リンク、config.toml `enable_confirmations=true`。テスト16件追加・verify pass。レビュー指摘1件（クエリ付きnextのエンコード壊れ→`new URL`化）修正済み。**注意: pending遮断は TKT-0230 結合まで未完＝0230完了までデプロイしない**。残: 実メールスモーク（hosted適用時）。
- TKT-0236 — 食材一覧（在庫）に選択チェックからの一括削除を追加。`deleteSelectedInventoryItems`（`.delete().eq("user_id").in("id", ids)`・買い物リスト `deleteSelectedShoppingItems` と同型）＋在庫側 `ListToolbar` に「選択削除」（`showDelete`）。確認は既存 `DeleteConfirmPanel` 経由（「N件の食材」明示・キャンセル可・0件時非活性）、成功後に notations 掃除/選択クリア/編集中なら resetForm/件数フィードバック。楽観的更新なし・undo なし（非ゴール）。テスト2件追加（計28件 pass）。verify pass。危険evalは `inventory_items` トークン過剰マッチ＋並行auth作業の同居＝静的記録で gate 全閉。残: 実機目視（選択→削除→消滅、キャンセル非削除）。
- TKT-0235 — 調理ビューア写真エリアに写真⇔動画切替＋YouTubeインライン再生（SPEC-0226 完結）。親 state `cookingMediaTab`（`openCookingViewer` で YouTubeあり=video へリセット）＋ CookingViewer に切替タブ（`aria-pressed`/tooltip）と `youtube-nocookie.com/embed/<videoId>` iframe（autoplay なし・title/allowFullScreen）。videoId は TKT-0234 の `findFirstYoutubeVideoId` 再利用、URL無しレシピは従来表示・切替UI非表示。「写真を隠す」トグルと共存（動画ありは「写真・動画を隠す」へ出し分け）。CSS は aspect-ratio 16/9。テスト2件追加（計63件 pass）。verify pass。危険evalは photo 語彙過剰マッチ＋並行auth作業の同居＝静的記録で gate 全閉。残: 実機目視（動画初期表示・切替・CSP violation なし・375px/PC幅）。
- TKT-0234 — YouTube埋め込み基盤（TKT-0235 の土台）。`web/src/lib/youtube.ts` 新設: `extractYoutubeVideoId`（URL パース＋ホスト完全一致5種・watch?v=/youtu.be/shorts/embed 対応・videoId `[A-Za-z0-9_-]{11}` のみ有効＝embed URL 連結安全）/ `findFirstYoutubeVideoId`（改行区切り source から先頭の videoId）。CSP（next.config.ts）に `frame-src https://www.youtube-nocookie.com` のみ追加（他ディレクティブ不変。frame-src 未定義のままでは iframe が default-src 'self' で全ブロックされる前提制約を解消）。テスト32件追加。verify pass。残: なし（実機確認は TKT-0235 側で実施）。
- TKT-0228 — 承認制ログインのDB土台（SPEC-0228 ①/⑥）。`profiles` テーブル新設 migration（status/role・auth.users トリガー自動作成・既存ユーザー approved backfill・admin RLS＝`is_admin()` security definer・insert/delete ポリシーなし）＋ runbook §10b に初代 admin 昇格 SQL。web/ コード無変更・テスト449件 pass・verify pass。レビュー指摘1件（トリガー email null 耐性）修正済み。**hosted 未適用**（TKT-0233 ゲートで `db push`＋実機スモーク消化）。
- TKT-0227 — 食材名の同義語辞書を設定画面から登録可能に（ユーザー辞書）。`user-synonyms.ts` 新設（localStorage `stock-master:user-synonym-groups:v1`・SSR-safe・型検証、parse=1行1グループ/区切り＝=、,・2語未満無視）＋ name-match.ts に `setUserSynonymGroups`（`user:${idx}` で静的辞書と名前空間分離・新Map差し替え・ユーザー辞書一致も score=2）。設定パネルに textarea＋保存＋有効グループ数フィードバック、web-mode-shell の useEffect で起動時ロード、保存で即時反映・空保存で解除。部分一致非同一視（SPEC-0222）は不変。テスト27件追加（計415件）。verify pass。制約: 端末ローカル（他端末非同期、DB同期は将来チケット）。残: 実機目視（設定UI・反映の体感）。
- TKT-0222 追補 — 類義語辞書に肉系13グループ追加（豚肉/豚こま/牛肉/牛こま/鶏もも/鶏むね/ささみ/ひき肉/豚・鶏・牛ひき肉/合いびき/ウインナー。グループ20〜32）。「豚こま肉=豚コマ」が辞書一致に。総称と切り方・肉種付き（豚肉×豚こま、ひき肉×豚ひき肉）は同一視しない方針を維持しテストで固定（+11件）。
- TKT-0226 — 買い物追加（不足材料選択）モーダルのPCワイド化（SPEC-0225 完結）。globals.css の `@media (min-width:1024px)` 末尾に追記のみ（マークアップ無変更）: `.shopping-shortage-modal` を `min(880px,94vw)`＋`height:auto`＋`max-height:calc(100vh-96px)`（従来512px固定の細長解消）、`.shopping-shortage-list` を `repeat(auto-fill, minmax(240px,1fr))` グリッド（880pxで2〜3カラム自動調整、overflow-y:auto 維持）、食材名の折返し対策。タブ/全選択/下部アクションの DOM・挙動不変・スマホ1カラム不変。テスト377件 pass・verify pass。危険evalは他チケット語彙の過剰マッチ＝静的記録で gate 全閉。残: 実機目視（カラム自動調整・候補少数時の縦幅）。
- TKT-0225 — 消費量調整モーダルのPCワイド化（SPEC-0225）。globals.css の `@media (min-width:1024px)` 末尾に追記のみ（マークアップ無変更）: `.consumption-modal` を `min(960px,94vw)`（従来640px）、`.consumption-list` を `repeat(2, minmax(0,1fr))` 2カラム＋`align-items:start`、材料名の折返し対策（`overflow-wrap`/`word-break`）。共通クラスで調理完了/履歴編集の両モーダルに効き、料理記録パネルは対象外。スマホ1カラム不変・375px非破壊。テスト377件 pass・verify pass。危険evalは他チケット語彙の過剰マッチ＝静的記録で gate 全閉。残: 実機目視（2カラムの見え方・sticky 非崩れ）。
- TKT-0224 — 買い物不足計算に表記ゆれマッチング適用（TKT-0222 適用・SPEC-0222 完結）。`inventoryAmountByNameAndUnit` のフィルタ `item.name === name` を `matchesIngredientName` に置換（unit 一致は維持）し、正規化/辞書一致する在庫量を合算。1関数の変更で3箇所（スケジュール追加起点モーダル・調理ビュー起点モーダル・材料カード不足バッジ）に波及。在庫「卵」×レシピ「たまご」が不足に出ない／卵1個×たまご3個→不足2個／部分一致（豚こま切れ肉×豚肉）は従来どおり不足＝買い忘れ防止／単位不一致は非合算、をテスト4件で固定（計377件）。INSERT 経路・schema 無変更。verify pass。危険evalはトークン過剰マッチ＝静的記録で gate 全閉。
- TKT-0223 — 消費量調整（調理完了/履歴編集）の在庫自動紐付けを表記ゆれ対応に（TKT-0222 適用）。`findMatchingStock` を name-match.ts に新設（分類・単位・在庫>0・matches=score>=2、複数候補はスコア降順=完全>正規化>辞書）し、`buildDraftsFromRecipeIngredients`（履歴編集）と `buildConsumptionDrafts`（調理完了）の完全一致 find を置換。在庫「卵」×レシピ「たまご」が自動選択、部分一致のみ（豚肉×豚こま切れ肉）は非選択をテスト固定。`ConsumptionEditor` のおすすめ optgroup をスコア降順ソート、`ConsumptionEditList`（履歴編集モーダル）もフラット→同 optgroup 構成に統一。在庫減算・保存経路（computeInventoryAdjustments）無変更。テスト5件追加（計373件）。verify pass。危険evalはテーブル名トークンの過剰マッチ＝静的記録で gate 全閉。残: 実機目視（optgroup 表示・自動選択の体感）。
- TKT-0222 — 食材名マッチングユーティリティ新設（TKT-0223/0224 の土台。SPEC-0222）。`web/src/lib/ingredients/name-match.ts` に `normalizeIngredientMatchName`（NFKC・小文字・空白除去・カナ→かな、長音保持）/ `matchesIngredientName`（正規化一致 or 辞書一致のみ true。**部分一致では true にしない**＝買い忘れ防止のユーザー確定方針をテストで固定）/ `ingredientNameMatchScore`（4完全>3正規化>2辞書>1部分>0不一致）。類義語辞書20グループ（卵=玉子=たまご等）を Map 化し O(1) 照合。部分一致は包含＋部分列（豚肉↔豚こま切れ肉、2文字以上、並び順専用）— サブエージェントの acceptance 逸脱（テスト側改変）をレビューで検出し実装側を修正。新規2ファイルのみ・既存変更ゼロ。テスト45件追加（計368件）。verify pass。危険evalは他チケット語彙の過剰マッチ＝静的記録で gate 全閉。
- TKT-0219 追補 — 調理ビューの写真をヘッダー小サムネから**左ペイン（材料）最上部の大判表示**へ移設（ユーザーフィードバック「小さすぎる」対応）。`cooking-pane-photo`＋`RecipeThumb size="hero"`（ペイン幅・16/9・角丸）、CookingViewer に `imageUrl`/`isPhotoOpen`/`onTogglePhoto` props 追加。トグルは開時=写真右上オーバーレイ、閉時=細バー。state/リセット挙動・Storage 無変更。verify pass。
- TKT-0221 追補 — ツールチップの裏回り/見切れを全面修正（ユーザーフィードバック対応）。原因3系統: ①`overflow-x:auto` 行の縦クリップ→並び/タブ/カテゴリ4行を PC幅で `flex-wrap` 化 ②sticky のスタッキングコンテキスト→`:has(:hover)` z昇格＋新バリアント `data-tooltip-pos="bottom-left/right"`（modal-close 14箇所・画面上端ボタンを下向き化、画面下端ナビは上向き化）＋汎用 `[data-tooltip]:hover{z-index:111}` ③クリップ不能箇所（30日カレンダー・縦スクロールリスト内・overflow:hidden 必須の小ボタン）は native `title` へフォールバック。クリック動作無変更。verify pass（368件）。残: 実機目視。
- TKT-0221 — 全ボタンへヘルプツールチップ付与（17ファイル・194箇所、`<button` 数=`data-tooltip` 数の突き合わせで漏れゼロ確認）。`.recipe-genre-more` 専用だったツールチップを `globals.css` の汎用 `[data-tooltip]`（`::after`・背景 `#0f172a`・`:hover`/`:focus-visible` 表示・z-index 110=モーダルbackdrop(100)より前面）へ一般化＋配置バリアント `data-tooltip-pos="bottom|left|right"` で画面端見切れ対策。文言は既存 `aria-label`（79箇所）と整合（aria主・tooltip視覚補助）、動的対象名は template literal。クリックハンドラ・レイアウト無変更＝属性付与＋CSSのみ。verify pass（323件）。photo_upload_storage/supabase_schema_change は語彙過剰マッチ＝manual-smokes/review に静的記録で gate 全閉。残: 実機目視（文言妥当性・端見切れ・モーダル上の前面表示。調整は属性値/CSS単独で可）。
- TKT-0220 — 調理ビュー手順内の分量バッジ（`.cooking-amount-chip`）の大さじ/小さじを背景SVGで識別可能に。`web/public/images/badges/` 新設＋`tablespoon.svg`（赤系「大」）/`teaspoon.svg`（青系「小」）を淡色（図形 opacity 0.12・文字 0.20）で作成し、`globals.css` の `[data-unit="大さじ"]`/`[data-unit="小さじ"]` に複合背景（`url(...) right center / auto 80% no-repeat, 既存単色`）＋`padding-right:24px` で文字非干渉。グレー系（その他単位）・ピル形状・`chipifyStep` 抽出ロジック無変更。verify pass（323件）。photo_upload_storage/supabase_schema_change は「image」語の過剰マッチ（静的SVG＋CSSのみ）＝manual-smokes/review に静的記録で gate 全閉。残: 実機目視（淡さ・被り。調整はCSS単独で可）。
- TKT-0219 — 調理ビュー（`cooking-overlay` 全画面）ヘッダー左上（戻るボタン直後）にレシピ登録写真を表示し開閉トグルを追加。state `isCookingPhotoOpen`（初期 開、`openCookingViewer` で開にリセット）＋`cooking-overlay-photo-block`、写真は既存署名URL `recipeImageUrls.get(id)` を `RecipeThumb` で再利用（未登録時はプレースホルダ）。トグルに `aria-label`/`aria-expanded`/`title`。CSS はサムネ 56×42px（720px以上 72×54px）・閉時は幅をトグル分へ縮小＋`flex-shrink:0` でタイトル/ヘッダー操作と非干渉。調理完成写真（cooking_history.photos）・Storage/署名URL取得ロジック無変更。テスト2件追加（開閉サイクル・開き直しリセット）で計323件 pass。verify pass。supabase_schema_change/photo_upload_storage は語彙過剰マッチ（表示のみ）＝manual-smokes/review に静的記録で gate 全閉。残: 実機スモーク（PC/スマホ幅でのヘッダー非重なり目視）。
- TKT-0218 — レシピ一覧（`RecipeList`）の各カードの写真サムネ（`RecipeThumb`）を `<button className="recipe-thumb-button">` でラップし、クリックで `event.stopPropagation()` → 既存 `onCook`（=`openCookingViewer`）を呼んで調理ビュー全画面を開くよう変更。カード本体クリック＝選択（詳細パネル）の従来挙動と他カード内ボタンは不変（二重発火なし）。aria-label「{recipe.name} の調理ビューを開く」でキーボード/SR 操作可。`globals.css` は `.recipe-card > .recipe-thumb` 参照の3セレクタ（base/1024px/640px）を新ラッパー構造へ追随＋hover/focus アフォーダンス。署名URL（`imageUrls.get`）はそのまま再利用＝Storage/取得ロジック無変更。テスト追加（サムネボタンクリックで「調理ビューア全画面」ダイアログ表示、既存55件も pass）。verify pass（lint/typecheck/test/build＋policy 全pass）。supabase_schema_change/photo_upload_storage は `web/`＋`recipes`/`image`/`Storage` の語彙過剰マッチ（写真クリック導線+UIのみ＝schema/migration/auth/RLS/写真Storage 無変更）、ticket `required_gates` のとおり manual-smokes/review 不要（TKT-0213/0215/0216/0217 と同運用）。残: 実機スモーク（`pwa_mobile_ui`／サムネタップ→調理ビュー一致・カード領域タップ→選択維持・カード内ボタン二重発火なし・Tab/Enter で開く）。
- TKT-0217 — ホーム（`HomeDashboard`）の「お気に入り/最近作ったレシピ」写真カードのクリックを、レシピ一覧タブ移動（`selectShellLeaf("recipes","recipes")`）から該当レシピの調理ビュー直行（`requestViewRecipe(recipe.id)`、origin 既定 `"recipes"`）へ置換。既存 `useShellNavigation` インフラ再利用で `pendingRecipeId` 経路（`recipe-meal-workspace.tsx` が `openCookingViewer` 受け）に委譲＝新規 state/ルーティングなし、`cooking-history-board.tsx` と同方式。閉じた後の戻り先は origin により「献立・レシピ > レシピ」。サマリーカードの `selectShellLeaf` 遷移は据え置き。`home-dashboard.test.tsx` のモックに `useShellNavigation` 追加＋カードクリックで `requestViewRecipe("new")` を検証。verify pass（lint/typecheck/test/build＋policy 全pass）。supabase_schema_change/photo_upload_storage は `web/`＋`recipes`/`image`/`Storage` の語彙過剰マッチ（クライアント遷移のみ＝schema/migration/auth/RLS/写真Storage 無変更）、ticket `required_gates` のとおり manual-smokes/review 不要（TKT-0213/0215/0216 と同運用）。残: 実機スモーク（`pwa_mobile_ui`／カード→調理ビュー一致→閉→レシピへ戻る）。
- TKT-0215 — 食材カード（`.stock-item`）に、登録写真があるカードのみ背景画像を薄く敷いて視認性向上（36pxアイコンが小さい問題対応）。`inventory-board.tsx` でカード描画時に `resolveItemImageUrl` の戻り値を変数 `itemImageUrl` に取り `IngredientIcon` と共有（二重呼び出し回避）、URLがある時だけ `data-has-bg="true"`＋CSS変数 `--bg-image` を付与（無いカードは従来表示）。CSS: `.stock-item[data-has-bg]` に `linear-gradient` オーバーレイ（不透明度0.88）＋ `background-size:cover` で背景敷設、期限切れ/期限近は併用セレクタ（単独 `:has` より高詳細度）でオーバーレイを赤/オレンジ寄りにして識別維持、Canvas幅（白背景）は別途白寄りオーバーレイ。`background-attachment:fixed` 不使用（モバイル崩れ/スクロール負荷回避）、背景は装飾扱いで aria 追加なし。verify pass（lint/typecheck/test/build＋policy 全pass）。photo_upload_storage/supabase_schema_change は `image/photo` 等の語彙過剰マッチ（表示のみ＝既存解決済み署名URL再利用・Storage/schema/auth/RLS 無変更）、spec_ready は単票変更で非該当（`related_specs: []`）＝manual-smokes/review 不要。残: 実機/DevTools の目視（明暗写真でのテキスト/バッジ/ステッパー可読性、期限トーン併存の識別、375px/Canvas幅の非崩れ）。
- TKT-0214 — 料理記録カード（`cooking-history-board.tsx` の `HistoryDateGroup` / `.history-item`）のUI刷新。複数写真の2枚目以降を本文右側のサムネ格子（`HistoryPhotoGrid`／`repeat(auto-fill,minmax(54px,1fr))` で幅に応じ列可変、上限6枚超は最後のサムネに `+N` オーバーレイ）に表示し、1枚目は従来の左80px枠＋`📷N` バッジを維持。操作は**行クリック→写真モーダル**に集約（`<article>` を `role="button"` 化せず透明な実 `<button.history-item-hitarea>` を重ねてキーボードでも開ける＝jsx-a11y `no-noninteractive-element-to-interactive-role` 回避）。「編集」は `stopPropagation()` で写真モーダルを開かず編集モーダルのみ。カードの「レシピを見る」を廃止し `CookingRecordViewModal` 内へ移設（`onViewRecipe`、`item.recipe_id` がある時のみ→`requestViewRecipe(recipe_id,"cooking")`）。CSS: `.history-item.has-photo-grid` 3カラム化、格子の `pointer-events:none`＋サムネ `auto` で余白クリックを下のヒットエリアへ通し `padding-top:34px` で編集ピル回避、`@media(max-width:640px)` で格子を畳み従来の左1枚＋縦積みへ縮退。署名URLは既存 `photoUrlMap`（`useCachedSignedUrls`）再利用で新規発行なし。テスト計13件 pass（カードのレシピ導線廃止／モーダル移設・行/サムネクリックでモーダル・`+N`・編集の伝播停止を追加）。verify pass（lint/typecheck/test/build＋policy 全pass）。photo_upload_storage/supabase_schema_change は `写真|image`/テーブル名の語彙過剰マッチ（表示のみ＝Storage/schema/auth/RLS/AI/CSV 無変更）、spec_ready は単票変更で非該当（`related_specs: []`）。残: 実機/DevTools の目視（PC=列可変＆編集ピル非重なり＆`+N`、スマホ=格子縮退＆`📷N`、行/編集/モーダル内レシピ遷移の挙動）。
- TKT-0213 — 料理・記録カレンダーに献立スケジュール（`meal_schedules`）を表示する土台（TKT-0214 の前提）。`page.tsx` の server fetch 済み `mealSchedules` を `CookingHistoryBoard` に `initialMealSchedules` として配線（新規Supabase問い合わせなし）。カレンダーセルは `items.length===0 && 当日に予定あり` のときだけ「予定」ドット（`data-kind="schedule"`／`#84cc16`）を点灯し凡例「予定のみ」と意味一致。`HistoryDateGroup` に `schedules?: MealSchedule[]` を追加し選択日の**未完了**予定を記録行と別系統の予定行（食事区分バッジ＋レシピ名＋「未完了」＋`recipe_id` あれば「レシピを見る」→`requestViewRecipe(recipe_id,"cooking")`）として描画、件数は `items+schedules` 合算・空表示は「記録も予定も無い日」のみ。テスト追加（未完了予定の予定行表示・完了済み非表示・レシピ導線）。verify pass（lint/typecheck/test/build＋policy 全pass）。supabase_schema_change は `meal_schedules` 語の過剰マッチ（表示のみ＝schema/migration/auth/RLS/Storage 無変更）、spec_ready は単票で非該当（`related_specs: []`）。残: 実機/DevTools 375px の目視（予定のみ/記録のみ/両方/どちらも無い の4パターン）。
- TKT-0212 — スケジュール削除時に、その登録で追加した**未購入**の買い物リスト項目を確認のうえ連動削除（危険変更＝schema）。`shopping_items` に `meal_schedule_id` を新設（migration `20260609120000_shopping_items_meal_schedule_link.sql`：列追加＝nullable後方互換、複合FK `(meal_schedule_id,user_id)→meal_schedules` の**列指定** `on delete set null (meal_schedule_id)`＝既存`20260523095800`踏襲、index `(user_id,meal_schedule_id)`、RLS既存`shopping_items_*_own`の範囲内で新規不要）。アプリ: `addScheduleEntry` 戻り値を `string|null`（作成献立id）化（既存呼び出しは無視で非破壊）、不足モーダルの紐付け state `shortageSelectionScheduleId` をスケジュール起点（`assignScheduleFromRecipe`/`addScheduleFromPicker`）のみ set・レシピ詳細「買い物へ」はnull、`confirmRecipeShortageSelection` の INSERT に `meal_schedule_id` 追加。`deleteSchedule` は献立削除の**前**に `shopping_items` を `meal_schedule_id/user_id/status=未購入` で delete→select("id")（FK set null より先に紐付けで消す・順序厳守）、失敗時は献立を消さず中断、削除件数を成功フィードバックに表示。確認文面（`scheduleDeleteMessage`）に「未購入の買い物リスト項目も一緒に削除します」を追記。テスト変更/追加（連動削除の絞り込み・確認文面・件数・紐付け保存／完了削除の順序非破壊）で計54件 pass。verify pass（lint/typecheck/test/build＋policy 全pass）。auth_and_rls_policy/photo_upload_storage/csv_import_migration は語彙過剰マッチ（RLSポリシー変更なし・写真/Storage/CSV無関係＝実変更は schema 列追加のみ、review/manual-smokes に記録）。spec_ready はイニシアチブ個別SPEC無し運用で非該当。残: **migration の本番適用（公開前ゲート・未適用だとINSERT失敗）**＋実機スモーク（連動削除・購入済み残存・null紐付けの非削除）。
- TKT-0211 — TKT-0210 のレシピ起点スケジュール登録フローに、登録成立直後の在庫不足チェック→既存買い物リスト追加を結線（Canvas版「登録時に在庫確認」UX再現）。`assignScheduleFromRecipe` の `addScheduleEntry` 成功時のみ `compareRecipeWithInventory` を実行し、不足があれば既存の不足選択モーダル（`shortageSelectionItems`/`shortageSelectionRecipeName`/`shortageSelectionTab` setter）を開く。買い物INSERTは既存 `confirmRecipeShortageSelection`（`shopping_items`）の再利用のみ＝新規DB導線・スキーマ無し。`addScheduleEntry` の戻り値を `Promise<boolean>` 化（既存呼び出しは戻り値無視で非破壊）、`recipeId` を呼び出し前に控えて成功時のnull化に備える。登録の成否と買い物追加は独立（「あとで」で閉じても献立は登録済み）。不足チェックは共通ヘルパー `openShortageModalForScheduledRecipe` に集約。**追補（ユーザー指摘反映）**: スケジュール「＋」のレシピ選択モーダルからの**新規追加**（`addScheduleFromPicker`）でも不足チェックが入るよう結線（当初はレシピ起点限定だったが、今週画面の＋でチェックが入らない指摘を受け対応）。スロットの「別のレシピに変更」(replace)とクイック追加フォーム(`saveSchedule`)は対象外。テスト5件追加（レシピ起点3件＋picker新規追加で買い物INSERT／replaceで非表示）。verify pass（lint/typecheck/test/build＋policy 全pass）。supabase_schema_change/photo_upload_storage は語彙過剰マッチ（git diff にテーブル直叩き・Storage/migration の新規追加なしを確認＝実schema/Storage 無変更）、spec_ready はイニシアチブ個別SPEC無し運用で非該当（report に記録）。残: 不足モーダルの表示/重なり・タブ/全選択・買い物反映・「あとで」キャンセルの実機スモーク。
- TKT-0210 — レシピ画面からのスケジュール登録を追加。レシピ詳細ヘッダー＝調理ビューア全画面のヘッダー（`cooking-overlay-schedule` ボタン。当初置いた `recipe-detail-panel` は `canvas-hidden-compat` で非表示のため移設）と各レシピカードの小ボタン（`schedule-icon-button`・`stopPropagation` で選択と非競合）の両方から、Canvas版相当の30日ミニカレンダー→朝/昼/晩選択モーダル（`schedule-add-modal`）で登録できる。モーダル backdrop は `schedule-add-backdrop`（z-index 95）で調理ビュー全画面(85)・消費/不足(90)の上・確認(100)の下に出す。モーダル専用状態 `scheduleAddRecipeId`/`scheduleAddSelectedDate` を新設しスケジュール画面状態と分離。日付は今日から30日を横スクロール表示（各セルに `mealSchedules` 集計の件数バッジ＋今日強調）、日付選択後に朝/昼/晩（`scheduleAddMealTypes`）を表示。登録は既存 `addScheduleEntry` をそのまま呼ぶだけ＝保存ロジック・`.from("meal_schedules")` 無変更で、成功時のモーダルクローズと成功フィードバックも既存分岐を再利用。CSS（`recipe-detail-actions`/`schedule-add-*`）追加。テスト2件追加（詳細ヘッダー起動→カレンダー→朝で insert payload 検証／カード小ボタン起動→晩で登録）。verify pass（lint/typecheck/test/build＋policy 全pass）。supabase_schema_change/photo_upload_storage は `recipes`/`image` への語彙過剰マッチ（実schema/Storage/auth 無変更）、spec_ready はイニシアチブ個別SPEC無し運用で非該当（report に記録）。残: レシピ詳細/カードからの起動・30日カレンダー横スクロール・朝/昼/晩登録・スマホ375px/PCの操作感・既存スケジュール機能非破壊の実機スモーク。
- TKT-0209 — スケジュール「＋」のレシピ選択モーダルに TKT-0208 の共通フィルターUI `RecipeFilterControls` ＋ `filterAndSortRecipes` を適用し、レシピ一覧と同じ検索/ソート/お気に入り体験へ統一（旧「レシピ名 includes」簡易検索を置換）。picker 専用状態 `pickerSearch`/`pickerSearchLogic`/`pickerSearchMode`/`pickerSort`/`pickerFavoriteOnly` を新設してレシピ一覧側状態と分離、`openPicker(slot)` ヘルパーに集約して開くたび初期化（前回検索語が残らない）。お気に入りは `is_favorite` 先行フィルタ→`filterAndSortRecipes`。登録は既存 `addScheduleEntry`/`replaceScheduleRecipe` をそのまま呼ぶだけ＝保存ロジック・`.from("meal_schedules")` 無変更。CSS: `.schedule-picker-modal` を PC（`min-width:720px`）で `min(720px,100%)` へ拡張＋`.schedule-picker-list` 2カラム化、スマホ幅は従来値温存。旧 `pickerQuery`/`.schedule-picker-search` 削除。テスト2件追加（共通検索＋お気に入り絞り込み／開き直しリセット）。verify pass（lint/typecheck/test/build＋policy 全pass）。supabase_schema_change/photo_upload_storage は `recipes`/`image` への語彙過剰マッチ（実schema/Storage/auth 無変更）、spec_ready はイニシアチブ個別SPEC無し運用で非該当（report に記録）。残: PC幅モーダル/2カラム・スマホ非破綻・検索/ソート/お気に入り切替の実機目視。
- TKT-0208 — レシピ検索/ソート/お気に入りUIを再利用可能なプレゼンテーショナルコンポーネント `web/src/components/recipe-filter-controls.tsx`（`RecipeFilterControls`）へ抽出（TKT-0209 のレシピ選択モーダル共通化の土台）。`recipe-meal-workspace.tsx` の `RecipeList` 内インラインJSX（検索対象タブ/AND-OR/検索入力＋クリア/ソート行/お気に入りチップ）と `searchTabs`/`sortTabs`・型 `RecipeSearchLogic`/`RecipeSearchMode`/`RecipeSort` を抽出先へ集約し、ワークスペースは import に切替（重複定義なし・状態と `filterAndSortRecipes` はワークスペース側のまま）。件数行 `recipe-count-row` は呼び出し側に残置。CSSクラス名・マークアップ不変＝見た目/挙動の純粋抽出。テスト `recipe-filter-controls.test.tsx` 4件追加。verify pass（lint/typecheck/test/build＋policy 全pass）。supabase_schema_change/photo_upload_storage は `recipes`/`image` への語彙過剰マッチ（実schema/Storage/auth 無変更）、spec_ready はイニシアチブが個別SPEC無し運用のため非該当（いずれも report に記録）。残: 任意のレシピ一覧目視スモーク。
- TKT-0206 — 写真アップロード全7経路（`inventory-board` 料理写真/在庫画像/ユーザー画像・`cooking-record-edit-modal`・`recipe-meal-workspace`・`recipe-image-upload` のコピー/アップロード）の `upload()` options に `cacheControl: "31536000"` を付与＋最小クライアント型に `cacheControl?` 追加＋テスト3件追従。**実Supabaseプローブ検証で当初目的は未達と判明**: メタには `cacheControl: max-age=31536000` が保存されるが、非公開バケットの**署名URL配信レスポンスにはブラウザ向け `cache-control` が伝播しない**（GETヘッダ無し/HEAD `no-cache`、`expires` は署名URL TTL=30分連動）。ブラウザキャッシュの実レバーは署名URL TTL＋同一URL再利用（TKT-0203/0204/0205）であり本変更ではない。ユーザー判断で「無害なので変更は残し事実を記録して完了」。schema/RLS/auth/バケット無変更（supabase_schema_change は語彙の過剰マッチ）。詳細は learnings（2026-06-07）。残: 撮影UIの実機目視（カメラ撮影・プレビュー・撮り直し）／体感改善は別チケット（署名URL TTL 延長＝セキュリティ要検討）。
- TKT-0205 — 料理履歴の完成写真を `page.tsx` のサーバ署名（`createSignedUrl`/30分TTL）依存からクライアント共有キャッシュ（TKT-0203 `useCachedSignedUrls`）解決へ移行。`cooking-history-board`/`cooking-record-edit-modal` で `storage_path`→URL を Map 解決、「写真あり」判定を `signed_url` 有無から `storage_path` 有無へ統一、未解決時フォールバック表示。`router.refresh()` 後の再署名→再DLを解消。型 `cooking-history/types.ts` は `signed_url` を `@deprecated`（互換optional維持）。テストは browser client/`useCachedSignedUrls` モック追加で追従。読み取り経路のみ＝schema/RLS/Storage設定/アップロード経路 無変更（supabase_schema_change/photo_upload_storage は語彙の過剰マッチ／report・review に静的記録）。残: 実機スモーク（カレンダー/タイムライン/インサイト/編集モーダル/レシピプレビュー表示＋DevTools Network の `from cache` 確認＋RLS回帰）。
- TKT-0204 — レシピ/食材/料理写真候補の署名URL取得を共有キャッシュ（TKT-0203）経由に統一（`use-recipe-image-urls`/`use-cooking-photo-candidates` を `getCachedUserImageSignedUrl` に差し替え、`inventory-board` の自前 effect を共有フック `useCachedSignedUrls` に置換、画像差し替え/削除フローに `invalidateUserImageSignedUrl` 追加）。再マウントで同一URLを返しブラウザ画像キャッシュをヒットさせ再DLを解消。読み取り専用＝公開設定/RLS/Storage無変更（photo_upload_storage/supabase_schema_change/auth_and_rls_policy は語彙の過剰マッチ／report に静的記録）。残: DevTools Network でモード往復の disk/memory cache ヒットと差し替え後の旧画像非表示の手動確認。
- TKT-0203 — 画像署名URLの共有キャッシュ基盤 `web/src/lib/photos/signed-url-cache.ts` 新設（path→{url,expiresAt} の module キャッシュ＋満了5分前マージン再発行＋in-flight Promise dedup＋`getCachedUserImageSignedUrl`/`invalidateUserImageSignedUrl`/共有フック `useCachedSignedUrls`、vitest 5件）。既存 `createUserImageSignedUrl`/TTL をラップする薄い層で表示箇所は無変更（差し替えは TKT-0204）。読み取り専用＝Storage/RLS/auth無変更（photo_upload_storage は語彙の過剰マッチ／report に静的記録）。
- TKT-0201 — 全画面ビューの材料・調味料サブグルーピングUI（行クリック＋Cmd/Ctrl複数選択→ラベル隣「グルーピング」で同一 item_type 内に `group_index` 付与、サブグループ見出し「解除」/選択行「グループ解除」で 0 へ戻す、見出し自動採番=材料A/B/C・調味料あ/い/う、D&Dはドロップ先の group_index 継承、`sameIngredientOrder` に group_index 比較追加で位置不変でも確定有効化）。保存は `recipe_ingredients` の item_type/sort_order/group_index 更新に限定＝schema変更なし（supabase_schema_change は語彙の過剰マッチ／report・manual-smokes に静的記録）。残: 実機375pxでのサブグループ枠・選択/D&D共存の目視。
- TKT-0200 — 材料・調味料サブグループのDB土台（`recipe_ingredients.group_index` 0=未グループ追加＝危険変更/supabase_schema_change、`check(group_index>=0)`、型・取得順`item_type,group_index,sort_order`・保存ペイロード対応。UI未実装＝常に0保存で挙動不変、policy/RLS無変更）。残: リモートmigration適用と適用後のRLSライブ確認（manual-smokes.md 手順あり）。
- TKT-0199 — 全画面ビュー「並び替えを確定」に確認を追加（保存前に `requestDelete`/`DeleteConfirmPanel` を流用した確認、OK時のみ既存 `saveCookingReorder` 実行・やめるで未確定保持）。並び替えロジック・保存対象カラム無変更。残: 実機での文言・操作感確認。
- TKT-0198 — レシピ編集モーダルの材料・調味料を3本線ハンドルD&D並び替え（`moveIngredient` で immutable 入替・同一 item_type 内限定、保存は既存 `saveRecipe`→`sort_order` 採番流用）。schema/新規DB書き込み無変更。残: 実機スマホでのD&D操作感確認。
- TKT-0186 — 主要操作ボタンの sticky 固定。食材管理ヘッダーの「＋」(スマホ sticky/PC static)・手動追加モーダルの「在庫に追加」(`.form-actions` sticky bottom)・全モーダルの「×」(`.modal-close-button` を absolute→sticky top:14px+grid右上、`margin-bottom:-40px` で見た目維持)。実差分は globals.css のみ（schema/Storage 無変更）。残: 実機/DevTools 375px 目視。
- TKT-0185 — 食材管理の食材名行の文字重なり修正。`.item-title-row`(flex)の子 `h4` に `min-width:0` を補い flex 縮小＋ellipsis を機能させ、スマホは `@media(max-width:719px)` で `.stock-item` を3列grid-areas再定義＋`h4` nowrap/ellipsis 化（チップと重ならない）。実差分は globals.css のみ（schema/Storage 無変更）。残: 実機/DevTools 375px 目視。
- TKT-0184 — スマホ横はみ出しの一掃（土台）。globals.css 末尾に `@media (max-width: 719px)` を追記し `.consumption-row-controls`(min-width220px解除)/`.cooking-filter-row`(5→2列)/`.conversion-row`(2列折返し・モーダル内は維持)/各種グリッド列数を緩和＋`body overflow-x:hidden` 保険。実差分は globals.css のみ（schema/Storage 無変更／eval過剰マッチはreport記録）。残: 実機/DevTools 375px 目視。
- TKT-0188 — 料理記録 編集モーダルの写真UI改善（既存写真をサムネ右上×で確認なし削除予定化＝実削除は確定時・キャンセルで残る・「削除予定N件（元に戻す）」で救済、新規写真はファイル名→サムネ表示＋×取り消し、objectURLは useEffect クリーンアップで解放）。実差分は Storage/schema 無変更（既存削除/追加経路の再利用）。残: 実機での削除/復元・サムネ・スマホ幅目視。
- TKT-0183 — 料理完成写真2エリア（調理完了フローの料理記録パネル／料理記録の編集モーダル・複数対応）へファイルD&D登録＋Ctrl+V貼り付け（TKT-0187整合の追補）を適用（共通フック `useImageFileDrop` の `pasteAreaProps`/`isActive` も再利用、`handleNewPhotosChange` を `addNewPhotos(File[])` に分離、ハイライト/アクティブCSS追加）。実差分は Storage/schema 無変更（既存圧縮・`photos` 登録経路の再利用）。残: 実機でのPCドロップ/貼り付け目視。
- TKT-0187 — 画像エリアのクリックフォーカス＋クリップボード貼り付け（Ctrl+V）対応（共通フック `useImageFileDrop` に `onPaste`/フォーカス管理を追加・アクティブ時のみ貼り付け＝ページ全体のCtrl+Vを奪わない・既存画像の差し替えも対応）。実差分は Storage/schema 無変更。残: 実機での Ctrl+V 貼り付け目視。
- TKT-0176 — 食材画像の登録・差し替え・削除UI（同名食材の記憶／`user_ingredient_images` 新設・個別在庫画像優先の表示順）。危険変更（photo_upload_storage/auth_and_rls_policy）。残: 実機スモーク（圧縮品質・スマホUI・クロスユーザー拒否）。
- TKT-0177 — AI構造化でURL込み本文のリンク消失を修正（AIのsource優先＋structureで空なら本文から `https?://` 正規表現抽出のフォールバック、共通部品 `RecipeSourceLinks`）。残: 実機での貼付→保存→リンク表示の目視。
- TKT-0175 — 食材標準画像カタログ（自作SVG標準アイコン＋表記ゆれ吸収 resolver、未対応食材は絵文字フォールバック）。
- TKT-0174 — レシピ画像の登録・差し替え・削除UI（4:3/1280px/WebP圧縮で非公開Storage保存・DBはpathのみ・表示優先順位統一）。危険変更（photo_upload_storage/schema）。残: 実機スモーク（圧縮品質・スマホUI・クロスユーザー拒否）。
- TKT-0173 — ユーザー登録画像のDB列 `image_storage_path` ＋非公開 `photos` バケット流用・本人領域限定policy。
- TKT-0169 — ホームのビジュアル刷新（`HomeHero`＋おすすめ写真カード、画像ゼロでも崩れないフォールバック）。残: 実機目視・実画像配置後の見栄え。
- TKT-0167 — レシピお気に入り `recipes.is_favorite` 新設（schema+RLS＝危険変更、hosted へ `db push` 適用済み）。残: UI happy-path確認。
- TKT-0166 — PCレシピカード縦型化（プレースホルダ＋名前2行クランプ＋操作ボタンをホバー退避）。残: 実機目視。
- TKT-0165 — PC献立スケジュールを縦アジェンダ表示へ差し戻し（死に余白解消、CSSのみ）。残: 実機目視。
- TKT-0164 — 完了モーダル（ConsumptionEditor）のUI簡素化・1行高密度化（除外は消費量0へ一本化）。残: 実機スモーク。
- TKT-0162 — PCホーム/ダッシュボード新設（サマリー4枚＋`TodayDashboard`、PCのみ初期表示）。残: UI happy-path確認。※これで TKT-0157〜0162（PCデスクトップUI化）の主要チケット完了。
- TKT-0161 — 散在設定を設定画面（`settings-panel.tsx`）へ集約（Gemini APIキー/AI残量/ログアウト）。残: 実機スモーク（設定遷移・キー保存→AI実行・ログアウト）。
- TKT-0160 — 料理・記録のPC多カラム化（内部タブ持ち上げ・タイムライン複数カラム・カレンダー拡大）。残: 実機目視。
- TKT-0156 — 消費量調整画面で調味料が分類されない不具合修正（在庫category→レシピ材料 `item_type` 由来へ統一＋AI分類指示）。残: 実機目視・既存AI生成レシピの再分類要否。
- TKT-0155 — レシピ編集モーダルの材料行レイアウト改善（モーダル720px化＋単位列拡大＋nowrap、CSSのみ）。残: 手元dev目視。
- デザイン正本 — `docs/design/pc-design-language.md` 新設（PC幅トーンを Image #3＝indigo+白基調に統一。デザイントークン＋コンポーネント規定）。
